services:
  quartz-caddy:
    image: quartz-caddy:latest
    build:
      context: .
    ports:
      - "80:80"
    volumes:
      - ./quartz:/config/quartz
    environment:
      - OBSIDIAN_GIT_LINK='github.com'
      # - SSH_PUB_KEY=''
