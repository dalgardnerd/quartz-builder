services:
  quartz-caddy:
    image: quartz-caddy:latest
    build:
      context: .
    ports:
      - "80:80"
    volumes:
      - ./quartz:/config/quartz
      - ./caddy:/config/caddy
      - ./obsidian:/config/quartz/content
